const { createImages } = require('../../../services/mongoose/images');

const createImages = async (req, res, next ) => {
    try {
        const result = await.
    } catch (err) {
        next(err);
    }
}